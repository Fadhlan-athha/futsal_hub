<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap'); body { font-family: 'Poppins', sans-serif; }</style>
</head>
<body class="bg-gray-100">

    <div class="flex min-h-screen">
        
        <aside class="w-64 bg-gray-900 text-white hidden md:block fixed h-full z-10">
            <div class="p-6 text-center border-b border-gray-800">
                <h1 class="text-2xl font-bold text-emerald-400">Futsal<span class="text-white">Hub</span></h1>
                <p class="text-xs text-gray-400 mt-1">Admin Panel</p>
            </div>
            <nav class="mt-6">
                <a href="#" class="flex items-center py-3 px-6 bg-gray-800 text-white border-l-4 border-emerald-500">
                    <i class="fas fa-tachometer-alt w-6"></i> <span class="font-medium">Dashboard</span>
                </a>
                <a href="/" target="_blank" class="flex items-center py-3 px-6 text-gray-400 hover:bg-gray-800 hover:text-white transition">
                    <i class="fas fa-external-link-alt w-6"></i> <span class="font-medium">Lihat Website</span>
                </a>
            </nav>
        </aside>

        <main class="flex-1 ml-0 md:ml-64 p-8">
            <div class="flex justify-between items-center mb-8">
                <h2 class="text-2xl font-bold text-gray-800">Dashboard Overview</h2>
                <div class="bg-white px-4 py-2 rounded-lg shadow-sm flex items-center gap-2">
                    <div class="w-8 h-8 bg-emerald-100 rounded-full flex items-center justify-center text-emerald-600 font-bold text-xs">A</div>
                    <span class="text-sm font-bold text-gray-700">Admin</span>
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <div class="bg-white p-6 rounded-xl shadow-sm border-l-4 border-emerald-500">
                    <p class="text-sm text-gray-500">Total Pendapatan</p>
                    <h3 class="text-2xl font-bold text-gray-800">Rp <%= totalPendapatan.toLocaleString('id-ID') %></h3>
                </div>
                <div class="bg-white p-6 rounded-xl shadow-sm border-l-4 border-blue-500">
                    <p class="text-sm text-gray-500">Total Booking</p>
                    <h3 class="text-2xl font-bold text-gray-800"><%= bookings.length %> <span class="text-sm font-normal">Transaksi</span></h3>
                </div>
                <div class="bg-white p-6 rounded-xl shadow-sm border-l-4 border-purple-500">
                    <p class="text-sm text-gray-500">Jumlah Lapangan</p>
                    <h3 class="text-2xl font-bold text-gray-800"><%= lapangans.length %> <span class="text-sm font-normal">Unit</span></h3>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-sm overflow-hidden mb-8">
                <div class="p-6 border-b border-gray-100 flex justify-between items-center">
                    <h3 class="font-bold text-gray-800">Data Booking Terbaru</h3>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="bg-gray-50 text-gray-500 text-xs uppercase tracking-wider">
                                <th class="p-4 font-semibold">ID Group</th>
                                <th class="p-4 font-semibold">Lapangan</th>
                                <th class="p-4 font-semibold">Jadwal Main</th>
                                <th class="p-4 font-semibold">Total</th>
                                <th class="p-4 font-semibold">Status</th>
                                <th class="p-4 font-semibold text-center">Aksi</th>
                            </tr>
                        </thead>
                        <tbody class="text-sm divide-y divide-gray-100">
                            <% bookings.forEach(book => { %>
                                <% 
                                    let jamStart = parseInt(book.jam_mulai.substring(0, 2));
                                    let jamEnd = jamStart + book.durasi;
                                    let jamEndStr = (jamEnd < 10 ? '0' + jamEnd : jamEnd) + '.00';
                                %>

                                <tr class="hover:bg-gray-50 transition">
                                    <td class="p-4 text-gray-500 text-xs font-mono">
                                        #<%= book.group_id.split('-')[1] %>...
                                    </td>
                                    <td class="p-4">
                                        <div class="font-bold text-gray-800"><%= book.nama_lapangan %></div>
                                        <div class="text-xs text-gray-500"><%= book.nama_pemesan || 'User Demo' %></div>
                                    </td>
                                    <td class="p-4">
                                        <div class="text-xs text-gray-500 mb-1">
                                            <%= new Date(book.tanggal).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                                        </div>
                                        <div class="text-gray-800 font-bold bg-gray-100 inline-block px-2 py-1 rounded text-xs">
                                            <%= book.jam_mulai %> - <%= jamEndStr %>
                                        </div>
                                        <span class="text-[10px] text-emerald-600 font-bold ml-1">(<%= book.durasi %> Jam)</span>
                                    </td>
                                    <td class="p-4 font-bold text-gray-800">
                                        Rp <%= parseInt(book.total_bayar).toLocaleString('id-ID') %>
                                    </td>
                                    <td class="p-4">
                                        <% if (book.status === 'Lunas') { %>
                                            <span class="bg-green-100 text-green-700 px-3 py-1 rounded-full text-xs font-bold">Lunas</span>
                                        <% } else { %>
                                            <span class="bg-yellow-100 text-yellow-700 px-3 py-1 rounded-full text-xs font-bold animate-pulse">Pending</span>
                                        <% } %>
                                    </td>
                                    <td class="p-4 text-center">
                                        <div class="flex justify-center gap-2">
                                            <% if (book.status !== 'Lunas') { %>
                                                <form action="/admin/confirm/<%= book.group_id %>" method="POST">
                                                    <button class="bg-emerald-500 text-white w-8 h-8 rounded hover:bg-emerald-600 flex items-center justify-center shadow-sm" title="Konfirmasi">
                                                        <i class="fas fa-check"></i>
                                                    </button>
                                                </form>
                                            <% } %>
                                            <form action="/admin/delete/<%= book.group_id %>" method="POST" onsubmit="return confirm('Hapus booking ini?');">
                                                <button class="bg-red-500 text-white w-8 h-8 rounded hover:bg-red-600 flex items-center justify-center shadow-sm" title="Hapus">
                                                    <i class="fas fa-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</body>
</html>